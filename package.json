{
  "name": "andrewedgar-io",
  "version": "2.0.0",
  "description": "Andrew Edgar's portfolio website - refactored to TypeScript, HTML, and CSS",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm run clean && npm run build:ts && npm run build:copy",
    "build:ts": "tsc && vite build",
    "build:copy": "cp public/favicon.png dist/ && cp public/manifest.json dist/",
    "dev": "vite",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "serve": "firebase serve",
    "deploy": "npm run build && firebase deploy"
  },
  "keywords": [
    "portfolio",
    "typescript",
    "firebase"
  ],
  "author": "Andrew Edgar",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  }
}
